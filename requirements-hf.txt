# Optional: fallback local LLM server for clue generation on platforms
# without vLLM support (macOS has no vLLM wheels — see requirements-vllm.txt).
# Serves the same model via transformers instead, on Apple Silicon (MPS) or
# CPU. run_vllm.sh installs/uses this automatically when it detects macOS.
#
#   pip install -r requirements-hf.txt
#   ./run_vllm.sh
torch==2.13.0
transformers==5.15.1
accelerate==1.14.0
